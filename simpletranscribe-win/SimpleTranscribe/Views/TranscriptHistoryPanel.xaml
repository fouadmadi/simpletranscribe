<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="SimpleTranscribe.Views.TranscriptHistoryPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:SimpleTranscribe.Models">

    <Grid RowDefinitions="Auto,1,*">

        <!-- Header -->
        <Grid Grid.Row="0" Padding="12,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="History" FontWeight="SemiBold" VerticalAlignment="Center" />
            <Button x:Name="ClearAllButton" Grid.Column="1"
                    Content="Clear All"
                    Foreground="Red"
                    Click="ClearAll_Click"
                    Style="{StaticResource HyperlinkButtonStyle}"
                    FontSize="12" />
        </Grid>

        <!-- Separator -->
        <Border Grid.Row="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}" Height="1" />

        <!-- List -->
        <ListView x:Name="HistoryList"
                  Grid.Row="2"
                  ItemsSource="{x:Bind History.Entries, Mode=OneWay}"
                  SelectionMode="None"
                  Padding="0"
                  IsItemClickEnabled="False">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:TranscriptEntry">
                    <Grid Padding="4,6" ColumnDefinitions="*,Auto,Auto">

                        <StackPanel Grid.Column="0" Spacing="2">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <TextBlock Text="{x:Bind TimestampRelative}"
                                           FontSize="11"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Text="·"
                                           FontSize="11"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                <TextBlock Text="{x:Bind DurationLabel}"
                                           FontSize="11"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            </StackPanel>
                            <TextBlock Text="{x:Bind Text}"
                                       MaxLines="3"
                                       TextTrimming="CharacterEllipsis"
                                       TextWrapping="Wrap"
                                       FontSize="13" />
                        </StackPanel>

                        <!-- Copy -->
                        <Button Grid.Column="1"
                                Tag="{x:Bind Text}"
                                Click="CopyEntry_Click"
                                ToolTipService.ToolTip="Copy"
                                Padding="6"
                                Margin="2,0">
                            <FontIcon Glyph="&#xE8C8;" FontSize="13" />
                        </Button>

                        <!-- Delete -->
                        <Button Grid.Column="2"
                                Tag="{x:Bind Id}"
                                Click="DeleteEntry_Click"
                                ToolTipService.ToolTip="Delete"
                                Padding="6"
                                Margin="2,0">
                            <FontIcon Glyph="&#xE74D;" FontSize="13"
                                      Foreground="{ThemeResource SystemFillColorCriticalBrush}" />
                        </Button>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
